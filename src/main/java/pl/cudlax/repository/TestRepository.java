package pl.cudlax.repository;

import org.springframework.stereotype.Repository;

@Repository
public class TestRepository {
	/*
	 * @Inject private SessionFactory sessionFactory;
	 * 
	 * @SuppressWarnings("unchecked") public List<TestBazy> readList() { return
	 * sessionFactory.openSession()
	 * .createQuery("from pl.cudlax.domain.TestBazy").list(); }
	 * 
	 * public void save(TestBazy tb) { Session session =
	 * sessionFactory.openSession(); session.save(tb); session.close(); }
	 */
}
